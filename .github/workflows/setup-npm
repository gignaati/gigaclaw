- name: Setup Node
  uses: actions/setup-node@v4
  with:
    node-version: '20'
    registry-url: 'https://registry.npmjs.org'

- name: Install / Publish
  run: npm publish   # or npm install
  env:
    NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
